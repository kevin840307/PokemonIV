<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mndt.ghost.mndtivpokemon">

    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/joy_12"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service android:name=".WindowServer">

        </service>

        <activity
            android:name=".search.MobNameSearch"
            android:theme="@style/Theme.Transparent"></activity>

        <activity
            android:name=".search.MobSkillSearch"
            android:theme="@style/Theme.Transparent"></activity>

        <activity
            android:name=".dataview.MobIvView"
            android:theme="@style/Theme.Transparent"></activity>

        <activity
            android:name=".search.MobAppraiseSearch"
            android:theme="@style/Theme.Transparent"></activity>

        <activity
            android:name=".search.MobSaveSearch"
            android:theme="@style/Theme.Transparent"></activity>

        <activity
            android:name=".dataview.MobSaveView"
            android:theme="@style/Theme.Transparent"></activity>

        <activity
            android:name=".dataview.BuiltPictureView"
            android:theme="@style/Theme.Transparent"></activity>

        <activity android:name=".dataview.TeachView"></activity>

        <activity
            android:name="com.google.android.gms.ads.AdActivity"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|uiMode|screenSize|smallestScreenSize"
            android:theme="@android:style/Theme.Translucent" />

    </application>

</manifest>